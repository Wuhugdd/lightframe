# LightFrame · 项目总览文档

> ESP32-S3 + 8×32 WS2812 智能炫彩桌面音乐时钟
> 前后端分离架构，网页配置，模式可扩展

---

## 一、项目定位

**LightFrame / "光帧"** 是一个放在桌面上的像素矩阵装饰品，兼具实用性和可玩性：

- 显示时钟、日期、天气
- 随音乐跳动的频谱可视化
- 网页配置，手机/电脑都能改
- 模式可扩展，写个 JSON 就能加新效果

---

## 二、功能清单

### 基本功能（MVP，必须实现）

| 功能 | 说明 |
|------|------|
| WS2812 驱动 | 点亮灯板，支持逐像素 RGB 控制 |
| 数字时钟显示 | 显示 HH:MM，可选日期 MM/DD |
| 音频频谱可视化 | 麦克风采集 + FFT 计算，随音乐跳动 |
| 光敏自动调光 | 根据环境光自动调节亮度，深夜自动关灯 |
| Captive Portal 配网 | 首次使用网页配网，不用硬编码 WiFi |
| 按键操作 | 短按切换模式，长按进入配网 |
| NVS 断电记忆 | 重启后恢复上次的模式和亮度 |
| 网页配置界面 | 模式选择、亮度调节、功能开关 |
| 后端 API 服务 | FastAPI 提供帧数据和配置管理 |
| JSON 模式系统 | 通过 JSON 文件定义显示模式，无需改代码 |

### 拓展功能（MVP 完成后按兴趣添加）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 时钟+频谱组合模式 | 高 | 下半显示时钟，上半显示频谱 |
| 情绪频谱配色 | 高 | 低频暖色/高频冷色，视觉更炸裂 |
| 像素画板 | 高 | 网页手绘图案，实时推送到灯板 |
| 滚动文字 | 中 | 可配置内容/速度/颜色 |
| 天气显示 | 中 | 调用 OpenWeatherMap，显示温度和图标 |
| AI 每日语录 | 中 | 调用大模型生成文字，滚动显示 |
| 节拍检测 | 中 | 检测低音节拍，节拍到来时整屏闪烁 |
| 番茄钟 + 呼吸灯 | 低 | 专注计时，到点呼吸灯提醒 |
| 定时开关灯 | 低 | 用户设定自动开关时间 |
| 模式分享 | 低 | 导出 JSON 短链接，社区分享 |
| 声控小游戏 | 彩蛋 | 打地鼠/音波跑酷 |
| 像素宠物 | 彩蛋 | 在灯板上养一只流浪像素宠物 |

---

## 三、技术栈详解

### 3.1 硬件

**核心器件**

| 器件 | 型号/规格 | 用途 |
|------|-----------|------|
| 主控 | ESP32-S3 | WiFi + 蓝牙 + 双核 240MHz |
| 灯板 | 8×32 WS2812B | 256 颗 RGB LED，串行控制 |
| 麦克风 | PDM 数字麦克风（如 MSM261S4030H0R） | 采集环境音频 |
| 光敏传感器 | 光敏电阻 + 分压电路 | 检测环境亮度 |
| 按键 | 轻触开关 × 1 | 模式切换 / 进入配网 |
| 电源 | 5V 2A USB 供电 | 256 颗全亮约 1.5A |

**引脚分配（参考 `firmware/include/config.h`）**

| 功能 | 引脚 |
|------|------|
| WS2812 数据线 | GPIO 48 |
| PDM 麦克风 CLK | GPIO 42 |
| PDM 麦克风 DATA | GPIO 41 |
| 光敏传感器 ADC | GPIO 4 |
| 按键 | GPIO 0 |

**硬件注意事项**

- WS2812B 数据线建议串联 330Ω 电阻，防止信号反射
- 灯板电源和 ESP32 电源共地，但灯板 5V 不要直接接 ESP32 的 3.3V 引脚
- 全亮电流较大，使用至少 2A 的 USB 电源适配器
- 光敏电阻用 10kΩ 上拉分压，ADC 读取 0~4095

---

### 3.2 固件（ESP32 端）

**开发环境**

- 框架：Arduino（PlatformIO 管理）
- 语言：C++17
- 项目文件：`firmware/platformio.ini`

**模块划分**

| 文件 | 职责 |
|------|------|
| `main.cpp` | 主状态机，协调各模块 |
| `ws2812.cpp/.h` | WS2812 驱动，帧缓冲区管理 |
| `mic_fft.cpp/.h` | PDM 采集 + arduinoFFT 计算频谱 |
| `light.cpp/.h` | 光敏 ADC 读取 + 亮度平滑映射 |
| `network.cpp/.h` | WiFi 连接 + HTTP 请求后端帧数据 |
| `portal.cpp/.h` | Captive Portal 配网页面 |
| `storage.cpp/.h` | NVS 读写（WiFi 配置、当前模式、亮度） |
| `renderer.cpp/.h` | 将后端返回的 768 字节 RGB 数据填充到帧缓冲区 |

**关键设计**

- 使用 FreeRTOS 双任务：Task1 负责 HTTP 拉帧 + 缓存更新，Task2 负责驱动灯板刷新
- 模式分两类：
  - **实时模式**（时钟、频谱）：ESP32 本地计算，不请求后端，延迟低
  - **静态模式**（图片、文字）：每隔几秒请求一次后端，其余时间显示缓存帧
- FFT 使用 256 点，8 个频段，映射到灯板 8 列高度

**依赖库**

```ini
; platformio.ini
lib_deps =
    fastled/FastLED
    kosme/arduinoFFT
```

---

### 3.3 后端（Python 服务端）

**开发环境**

- 语言：Python 3.10+
- 框架：FastAPI + Uvicorn
- 存储：SQLite（通过 Python 内置 sqlite3）
- 项目入口：`backend/api/index.py`

**模块划分**

| 文件 | 职责 |
|------|------|
| `api/index.py` | FastAPI 入口，定义所有 API 端点 |
| `core/mode_registry.py` | 加载 `modes/` 目录下的 JSON 模式文件 |
| `core/pipeline.py` | 渲染管线，根据模式类型调用对应渲染器 |
| `core/ws2812_renderer.py` | 生成 8×32 RGB 帧，输出 768 字节二进制 |
| `core/config_store.py` | SQLite 读写设备配置 |
| `core/context.py` | 获取当前时间、天气等上下文数据 |

**API 端点**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/modes` | 列出所有可用模式 |
| GET | `/api/frame?mac=&mode=` | 返回 768 字节 RGB 帧数据 |
| POST | `/api/draw` | 接收画板像素数组并缓存 |
| GET | `/api/config/{mac}` | 获取设备配置 |
| POST | `/api/config/{mac}` | 保存设备配置 |

**启动方式**

```bash
cd backend
pip install -r requirements.txt
python api/index.py
# 服务运行在 http://0.0.0.0:8000
```

**依赖清单（`requirements.txt`）**

```
fastapi>=0.104.0
uvicorn>=0.24.0
Pillow>=10.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
```

---

### 3.4 前端（网页配置界面）

**技术选型**

- 纯静态 HTML + CSS + 原生 JS，无框架依赖
- 由后端 FastAPI 的 `StaticFiles` 挂载，访问路径 `/config/`
- 代码编辑器使用 CodeMirror 5（CDN 引入）

**页面划分**

| 文件 | 功能 |
|------|------|
| `config.html` | 主控制中心：模式选择、亮度、开关、实时预览 |
| `editor.html` | JSON 模式编辑器：写 JSON 定义自定义模式 |
| `draw.html` | 像素画板：32×8 网格涂色，推送到设备 |
| `style.css` | 全局样式，统一 UI 风格 |

**访问地址**

```
http://localhost:8000/config/config.html   # 控制中心
http://localhost:8000/config/draw.html     # 像素画板
http://localhost:8000/config/editor.html   # 模式编辑器
```

---

## 四、通信协议

### ESP32 ↔ 后端

```
ESP32 每隔 N 秒发起请求：
GET /api/frame?mac={设备MAC}&mode={当前模式ID}

后端响应：
Content-Type: application/octet-stream
Body: 768 字节二进制数据（8×32×3，R/G/B 顺序，行优先）

ESP32 收到后直接填充帧缓冲区，调用 FastLED.show() 刷新灯板。
```

### 网页 ↔ 后端

```
配置读写：GET/POST /api/config/{mac}   JSON 格式
模式列表：GET /api/modes               JSON 格式
画板推送：POST /api/draw               JSON 格式（256 个 [R,G,B]）
```

---

## 五、实施计划表

> 按难易程度排列，从最简单开始，每一步都能独立验证。

### 第一阶段：硬件调试（难度 ★☆☆☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 1 | 接线：ESP32-S3 + WS2812 灯板 | 上电不冒烟 | 0.5 天 |
| 2 | 点亮第一颗灯（FastLED Hello World） | 灯板显示红色 | 0.5 天 |
| 3 | 彩虹跑马灯 | 256 颗灯依次变色 | 0.5 天 |
| 4 | 接麦克风，串口打印 ADC 原始值 | 对着麦克风说话，数值变化 | 0.5 天 |
| 5 | 接光敏传感器，串口打印亮度值 | 遮住传感器，数值变化 | 0.5 天 |

### 第二阶段：固件基础（难度 ★★☆☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 6 | FFT 频谱计算，串口打印 8 个频段幅值 | 播放音乐，数值随节奏变化 | 1 天 |
| 7 | 频谱直接驱动灯板（不经过后端） | 灯板随音乐跳动 | 1 天 |
| 8 | 光敏调光：ADC 值映射到 FastLED 亮度 | 遮住传感器，灯板变暗 | 0.5 天 |
| 9 | 按键检测：短按打印日志，长按打印日志 | 按键响应正确 | 0.5 天 |
| 10 | NVS 存储：保存/读取一个整数（亮度） | 断电重启后亮度不变 | 0.5 天 |

### 第三阶段：WiFi 与配网（难度 ★★★☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 11 | WiFi 连接（硬编码 SSID/密码） | 串口打印 IP 地址 | 0.5 天 |
| 12 | HTTP GET 请求后端，打印响应内容 | 串口显示后端返回的 JSON | 0.5 天 |
| 13 | 接收后端 768 字节帧数据并显示 | 灯板显示后端生成的彩虹帧 | 1 天 |
| 14 | Captive Portal 配网页面 | 手机连 AP 后弹出配网页，输入 WiFi 后设备自动连接 | 1.5 天 |
| 15 | NVS 存储 WiFi 配置，断电后自动重连 | 重启后自动连接上次的 WiFi | 0.5 天 |

### 第四阶段：后端服务（难度 ★★☆☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 16 | 启动 FastAPI，`/` 端点返回 JSON | 浏览器访问 `localhost:8000` 有响应 | 0.5 天 |
| 17 | `/api/frame` 返回固定彩虹帧（768 字节） | curl 请求返回二进制数据 | 0.5 天 |
| 18 | JSON 模式注册表，加载 `modes/builtin/` | `/api/modes` 返回模式列表 | 0.5 天 |
| 19 | 频谱模式渲染（后端生成频谱帧） | 网页预览看到频谱图案 | 1 天 |
| 20 | SQLite 配置存储，读写设备配置 | POST 保存配置，GET 读取一致 | 0.5 天 |

### 第五阶段：网页配置（难度 ★★☆☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 21 | `config.html` 加载模式列表并显示 | 页面显示内置模式的胶囊标签 | 0.5 天 |
| 22 | 保存配置到后端，后端写入 SQLite | 点击"同步配置"，后端日志有写入记录 | 0.5 天 |
| 23 | 网页实时预览：抓取当前帧渲染到网格 | 点击"抓取当前帧"，网格显示对应图案 | 0.5 天 |
| 24 | 像素画板 `draw.html` 涂色并推送 | 画板涂色后点推送，`/api/draw` 返回 success | 0.5 天 |

### 第六阶段：完整联调（难度 ★★★☆☆）

| 序号 | 任务 | 验证方式 | 预计时间 |
|------|------|----------|----------|
| 25 | ESP32 启动时拉取配置，切换到对应模式 | 网页改模式，重启 ESP32 后灯板显示新模式 | 1 天 |
| 26 | 按键短按切换模式（本地 + 通知后端） | 按键后灯板切换，网页刷新后模式同步 | 1 天 |
| 27 | 时钟模式：ESP32 本地显示 HH:MM | 灯板显示正确时间 | 1.5 天 |
| 28 | 整体稳定性测试：连续运行 24 小时 | 无崩溃、无内存泄漏、WiFi 断线自动重连 | 1 天 |

### 第七阶段：拓展功能（难度 ★★★★☆，按兴趣选做）

| 序号 | 任务 | 预计时间 |
|------|------|----------|
| 29 | 情绪频谱配色（低频暖/高频冷） | 1 天 |
| 30 | 滚动文字模式（后端 PIL 渲染） | 1.5 天 |
| 31 | 天气显示（OpenWeatherMap API） | 1 天 |
| 32 | AI 每日语录（DeepSeek/Gemini API） | 1 天 |
| 33 | 节拍检测 + 整屏闪烁 | 1 天 |
| 34 | 番茄钟 + 呼吸灯提醒 | 0.5 天 |
| 35 | 模式 JSON 导出/导入分享 | 1 天 |

---

## 六、里程碑

| 里程碑 | 完成标志 |
|--------|----------|
| 🟢 硬件点亮 | 灯板显示彩虹跑马灯 |
| 🟢 频谱跳动 | 灯板随音乐实时变化 |
| 🟢 WiFi 联网 | ESP32 从后端拿到帧并显示 |
| 🟢 配网可用 | 手机扫 AP 完成配网，无需改代码 |
| 🟡 V1.0 完成 | 时钟 + 频谱 + 光敏 + 按键稳定运行 |
| 🟡 V2.0 完成 | 网页选模式，ESP32 切换成功，画板可用 |
| 🟠 拓展完成 | 天气 + 滚动文字 + 情绪频谱 |
| 🔵 开源发布 | README 完善，GitHub 公开 |
