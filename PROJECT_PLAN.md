# LightFrame · 开发计划

> 基于 ESP32-S3 + 8×32 WS2812 的智能炫彩桌面音乐时钟
> 学习 InkSight 工程化思想，实践前后端分离架构

---

## 项目定位

**LightFrame / "光帧"** - 智能 WS2812 桌面音乐时钟

- 硬件：ESP32-S3 + 8×32 WS2812 灯板 + PDM 麦克风 + 光敏传感器
- 特色：炫酷频谱可视化 + 自动调光 + 网页配置 + 多模式切换
- 定位：漂亮的桌搭装饰品，兼具实用性和可玩性
- 目标：通过这个项目，从裸机开发进阶到完整工程化项目

---

## 整体架构

```
┌─────────────┐    HTTP     ┌─────────────┐    ┌──────────┐
│  ESP32-S3   │ ←========→ │ FastAPI 后端 │ ←→ │ 网页配置 │
├─────────────┤            ├─────────────┤    └──────────┘
│ WS2812 驱动 │            │ 模式管理     │
│ 麦克风采集  │            │ 内容生成     │
│ FFT 频谱计算│            │ RGB 帧渲染   │
│ 光敏自动调光│            │ 配置存储(SQLite)│
│ Captive Portal 配网 │  │ 在线预览     │
└─────────────┘            └─────────────┘
```

### 职责分工（核心设计思想）

| 模块 | 职责 | 为什么这么分 |
|------|------|-------------|
| **ESP32 固件** | WS2812 驱动显示、麦克风 FFT 计算、光敏调光、配网、按键处理、NVS 存储 | **实时性要求高的放本地**：频谱必须本地计算，否则延迟太高 |
| **Python 后端** | JSON 模式管理、天气/语录生成、RGB 帧渲染、配置存储、API 服务 | **复杂内容生成放后端**：Python 生态丰富，换模式不用重刷固件 |
| **网页配置** | 用户交互：模式选择、参数调整、自定义内容、在线预览 | **用户体验好**：小白也能点几下就配置好 |

### 前后端分离的优势对比

| 维度 | 传统做法：ESP32 包办一切 | LightFrame：前后端分离 |
|------|-------------------------|-----------------------|
| 内存占用 | 大，要解析 JSON、渲染文字，容易爆内存 | 小，只存一帧 8×32×3 = 768 字节 RGB 数据 |
| 耗电 | WiFi 长时间连接，CPU 高负载 | 下载完就可以休眠，非常省电 |
| 扩展性 | 加新显示模式必须改代码、重刷固件 | 用户写 JSON 配置就能加新模式，不用刷机 |
| 开发体验 | 改一点就要编译、刷写，调试慢 | 后端改完刷新网页就生效，开发快 |
| 功能上限 | 受限于 ESP32 内存和计算能力 | 后端可以做很复杂的内容生成，比如调用大模型 |

---

## 功能规划

### 第一阶段 - MVP（最小可用版本）

> 先把核心跑通，能用，再完善

- [ ] **基础数字时钟** - 显示 HH:MM，可选显示 MM/DD 日期
- [ ] **实时音频频谱** - 麦克风采集音乐，FFT 计算，瀑布频谱随音乐跳动
- [ ] **光敏自动调光** - 根据环境光自动调节亮度，深夜自动关灯
- [ ] **Captive Portal 配网** - 第一次使用网页配网，不用硬编码 WiFi
- [ ] **按键操作** - 短按切换模式，长按进入配网
- [ ] **网页配置界面** - 模式选择、亮度调节、功能开关

### 第二阶段 - 进阶功能

> MVP 跑通了再加这些，让项目更完整更好玩

- [ ] **时钟+频谱组合模式** - 下半部分显示时钟，上半部分显示频谱，实用又酷炫
- [ ] **天气显示** - 后端调用天气 API，显示当前温度和天气图标
- [ ] **滚动文字** - 每日语录/新闻/倒计时，从右往左滚动显示
- [ ] **多种视觉主题** - 彩色渐变、呼吸灯、星空点阵、节日特殊皮肤
- [ ] **节拍检测** - 检测音乐低音节拍，节拍到来时整屏闪烁
- [ ] **定时开关** - 用户设定自动开灯时间、自动关灯时间
- [ ] **JSON 自定义模式** - 用户写 JSON 就能新增模式，不用改核心代码（完全借鉴 InkSight）
- [ ] **网页实时预览** - 改完配置，在网页上就能看到灯珠显示效果

---

## 项目目录结构

```
lightframe/
├── README.md              # 项目英文介绍（对外）
├── README_ZH.md           # 项目中文介绍（对外，放成品照片）
├── PROJECT_PLAN.md        # 开发计划（这个文件）
├── LICENSE                # MIT 开源许可证
├── .gitignore             # Git 忽略规则
├── CLAUDE.md              # 给 AI 助手的开发指引
├── firmware/              # ESP32-S3 固件 (PlatformIO)
│   ├── include/
│   ├── src/
│   │   ├── main.cpp       # 主状态机
│   │   ├── main.h
│   │   ├── ws2812.cpp    # WS2812 驱动
│   │   ├── ws2812.h
│   │   ├── mic_fft.cpp   # 麦克风采集 + FFT 计算
│   │   ├── mic_fft.h
│   │   ├── light.cpp     # 光敏传感器 + 自动调光
│   │   ├── light.h
│   │   ├── network.cpp   # WiFi + HTTP 请求
│   │   ├── network.h
│   │   ├── portal.cpp    # Captive Portal 配网
│   │   ├── portal.h
│   │   ├── storage.cpp   # NVS 配置存储
│   │   ├── storage.h
│   │   ├── renderer.cpp # RGB 帧渲染到 WS2812
│   │   └── renderer.h
│   └── platformio.ini    # 开发板配置 + 依赖
├── backend/               # Python FastAPI 后端
│   ├── api/
│   │   └── index.py      # FastAPI 入口 + API 端点
│   ├── core/
│   │   ├── mode_registry.py   # 模式注册表，加载 JSON 模式
│   │   ├── pipeline.py        # 统一生成+渲染管线
│   │   ├── config_store.py    # SQLite 配置存储
│   │   ├── context.py         # 获取时间/天气等上下文
│   │   └── ws2812_renderer.py # 渲染成 8x32 RGB 帧
│   ├── modes/             # JSON 模式定义
│   │   ├── builtin/      # 内置模式
│   │   └── custom/       # 用户自定义模式
│   ├── tests/            # 单元测试（可选）
│   ├── requirements.txt  # Python 依赖清单
│   └── .env.example      # 环境变量示例（API Key 等）
├── webconfig/            # 静态网页配置
│   ├── config.html      # 设备配置页
│   ├── preview.html      # 在线预览
│   ├── editor.html       # 自定义模式编辑器
│   └── style.css
└── docs/
    ├── hardware.md       # 硬件清单 + 接线图
    ├── setup.md          # 本地部署教程
    └── images/           # 项目截图和预览图
```

---

## 关键技术点

### 1. 频谱计算

- **推荐库**：[kosme/arduinoFFT](https://github.com/kosme/arduinoFFT) - 成熟易用
- **大致流程**：
  1. ADC 采集 256/512 个音频样本
  2. arduinoFFT 库计算 FFT
  3. 将频率分成 8/16 个频段
  4. 每个频段取幅值，映射到灯珠高度
  5. 按颜色渐变显示到矩阵上，做瀑布流下效果

### 2. 前后端通信协议

#### 获取显示帧

```http
POST /api/frame
Content-Type: application/json

{
  "mac": "设备MAC地址",
  "mode": "当前模式ID",
  "width": 8,
  "height": 32
}
```

**响应**：返回 `8*32*3 = 768 字节` 二进制 RGB 数据。
每个像素 3 字节，顺序是 R → G → B。ESP32 收到直接填缓冲区，一次性刷新 WS2812，非常简单。

#### 获取设备配置

```http
GET /api/config/{mac_address}
```

**响应示例**：

```json
{
  "active_modes": ["CLOCK", "CLOCK_SPECTRUM"],
  "default_brightness": 80,
  "spectrum_enabled": true,
  "auto_dimming": true,
  "color_scheme": "rainbow"
}
```

### 3. JSON 模式定义示例

**时钟+频谱模式**示例：

```json
{
  "mode_id": "CLOCK_SPECTRUM",
  "display_name": "时钟+频谱",
  "cacheable": false,
  "content": {
    "type": "computed",
    "source": "time"
  },
  "layout": {
    "clock_area": "bottom",
    "clock_height": 8,
    "spectrum_height": 24,
    "color_scheme": "rainbow",
    "clock_color": "#ffffff"
  }
}
```

用户新增模式只需要加一个 JSON 文件，不用改核心代码，重启后端就生效。

### 4. Captive Portal 配网流程

1. 设备启动，检查有没有 WiFi 配置
2. 如果没有配置，或者用户长按按键，ESP32 开启 AP
3. AP 名称：`LightFrame-XXXXX`（后五位是 MAC 后缀）
4. 用户手机连接这个 AP，会自动弹出配网页（Captive Portal）
5. 用户输入 WiFi SSID、密码、后端地址
6. ESP32 保存到 NVS，重启，连接 WiFi

### 5. 光敏自动调光算法

- 读取光敏电阻 ADC 值
- 根据 ADC 值映射亮度范围（比如 0-100）
- 如果低于阈值，关闭所有灯珠
- 平滑渐变，不要突变，看起来更舒服

---

## 分步实施计划

| 阶段 | 任务 | 预计时间 | 状态 |
|------|------|---------|------|
| **Step 1** | 硬件调试：接线，单独调通 WS2812、麦克风、光敏 | 1-2 天 | ⏸ 未开始 |
| **Step 2** | 固件模块化：按上面目录拆分模块，实现主状态机 | 3-5 天 | ⏸ 未开始 |
| **Step 3** | 后端搭建：FastAPI 架子，写 API 端点，SQLite 配置存储 | 2-3 天 | ⏸ 未开始 |
| **Step 4** | 模式系统：实现 JSON 模式加载，实现基础内置模式 | 2-3 天 | ⏸ 未开始 |
| **Step 5** | 网页配置：写简单配置页，保存配置，设备拉取生效 | 1-2 天 | ⏸ 未开始 |
| **Step 6** | 文档完善：写 README、硬件指南、部署教程，拍成品照片 | 1 天 | ⏸ 未开始 |

---

## 学习收获

通过这个项目，可以系统地从裸机开发进阶到工程化项目，你会学到：

1. ✅ **模块化编程思想** - 按功能拆分模块，每个模块只做一件事
2. ✅ **前后端分离架构设计** - 真正理解为什么要分离，优势在哪里
3. ✅ **JSON 驱动的可扩展系统** - 设计框架让用户不用改核心代码就能扩展
4. ✅ **完整开源工程规范** - README / LICENSE / .gitignore 这些文件的作用和写法
5. ✅ **入门数字信号处理** - 亲手做 FFT 频谱计算，理解信号处理
6. ✅ **用户体验设计** - 从用户角度思考，做小白友好的配网和配置流程

---

## 给开发者的建议

### 学习路线（如果你是跟着这个项目练习）

1. **先硬件后软件**：先把每个硬件单独调通，再集成。灯珠能亮，麦克风能读，光敏有变化，再往下走。
2. **先最小可用，再扩展功能**：先做出一个能显示时钟能显示频谱的版本，再加天气、滚动文字这些花活。
3. **模仿成熟项目**：InkSight 的架构设计经过验证，直接借鉴它的模式注册表、管线流程、配置存储思路，不用自己重新发明轮子。
4. **早点写文档**：做到哪写到哪，不要等到最后才写 README。

### 常见坑点

- **WS2812 电平**：ESP32 的 3.3V 电平足够驱动大部分 WS2812，不用转 5V（有些模块需要，可以加个电平转换）
- **电源功率**：8×32=256 颗灯珠，全亮电流不小，确保你的 USB 电源能供电
- **FFT 内存**：ESP32-S3 内存足够，不用担心，用 256 点 FFT 就够了
- **WiFi 配网失败**：做好错误处理，失败了回到配网模式，让用户重新配

---

## 备注

- 硬件使用手头已有 ESP32-S3 + 8×32 WS2812
- 架构设计灵感来自 [InkSight/墨鱼](https://github.com/datascale-ai/inksight)，适配 LED 矩阵硬件
- 项目完成后可以开源到 GitHub，非常棒的开源桌搭项目，肯定会有人喜欢
